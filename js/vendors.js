// inputs //
const allInputs = document.querySelectorAll(".form__section__input"); 

// output
const allOutputs = document.querySelectorAll(".output__paragraph__span"); 
const outputSection = document.getElementById("output-section");
const productsPerMonthOutput = document.getElementById("products-per-month-output");
const incomeBruttoOutput = document.getElementById("income-brutto-output");
const costOfRunningCompanyOutput = document.getElementById("running-company-costs-output");
const incomeNettoOutput = document.getElementById("income-netto-output");

// form
const form = document.getElementById("form");

// buttons //
const submitBtn = document.getElementById("submit-btn");
const closeOutputBtn = document.getElementById("close-output-btn");
const saveAsOutputBtn = document.getElementById("saveAs-output-btn");

// animated elements by JS // 
const titleSpan = document.getElementById("title__span");

// notifications // 
const notificationBack = document.getElementById("notification-back");
const notificationFront = document.getElementById("notification-front");
const notificationFrontSpan = document.getElementById("notification-front__span");
const notificationFrontTitle = document.getElementById("notification-front__title");
// notifications - ask for restore //
const notificationBackAsk = document.getElementById("notification-back-ask");
const notificationFrontAsk = document.getElementById("notification-front-ask");

// buttons ask for restore 
const buttonYes = document.getElementById("but")

